from sys import argv # pulled the argv module from sys

script, user_name = argv # assigned the file name to script.
phat = '>' # created a string value for the variable

print "Hi %s, I'm the %s script." % (user_name, script)
print "I'd like to ask you a few questions."
print "Do you like me %s?" % user_name
likes = raw_input(phat) #allows you to type in a string.  same below.

print "Where do you live %s?" % user_name
lives = raw_input(phat)

print "What kind of computer do you have?"
computer = raw_input(phat)

print """
Alright, so you said %r about liking me.
You live in %r. Not sure where that is.
And you have a %r computer.  Nice.
""" % (likes, lives, computer) 
# i dont think changing the argument to a different word is what was meant by changing 
# the argument.
